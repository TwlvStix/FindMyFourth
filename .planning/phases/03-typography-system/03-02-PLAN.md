---
phase: 03-typography-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/profile/create_profile/create_profile_widget.dart
  - lib/user_auth/sign_up_account/sign_up_account_widget.dart
  - lib/chat_group/game_chat_details/game_chat_details_widget.dart
  - lib/profile/edit_profile/edit_profile_widget.dart
  - lib/main_function/create_game/create_game_widget.dart
autonomous: true
---

<objective>
Migrate the 5 worst offender screens from hardcoded typography to AppTypography/AppText, eliminating anti-patterns and establishing consistent text hierarchy.

Purpose: Fix the screens with lowest typography compliance (15-30%), demonstrating migration pattern and achieving immediate visual impact.
Output: 5 high-traffic screens with 85%+ typography adoption, anti-patterns eliminated, consistent text hierarchy established.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 3 Plan 1 - Foundation and migration guide
@.planning/phases/03-typography-system/03-01-SUMMARY.md
@.planning/phases/03-typography-system/MIGRATION-GUIDE.md

# Phase 1 audit - specific screen issues
@.planning/phases/01-foundation-audit/typography-inconsistencies.md

# Design system
@lib/core/design_tokens/typography.dart
@lib/core/widgets/app_text.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate create_profile_widget.dart (15% compliant → 85%+ target)</name>
  <files>lib/profile/create_profile/create_profile_widget.dart</files>
  <action>
**Current state**: 16+ hardcoded fontSize values (10-24px range), heavy manual GoogleFonts.outfit usage, inconsistent form labels (14, 15, 16px).

**Migration strategy**:
1. Find all Text widgets with hardcoded fontSize or manual GoogleFonts calls
2. Replace using migration guide mapping:
   - Screen title (fontSize: 24) → AppText.screenTitle
   - Form section headers (fontSize: 18-20) → AppText.sectionHeader
   - Form labels (fontSize: 14-16) → AppText.label
   - Helper text (fontSize: 12-14) → AppText.helper or AppText.caption
   - Body text (fontSize: 15-16) → AppText.body or AppText.bodySmall
   - Small text (fontSize: 10-12) → AppText.caption

3. Remove ALL GoogleFonts.outfit() calls - not in design system
4. Remove AppTheme.override anti-patterns
5. Ensure form label consistency: all form labels use AppText.label
6. Add color only where needed (use AppColors, not Colors.*)

**Do NOT**:
- Change layout or spacing
- Modify functional logic
- Add new features
- Change widget structure

**Expected outcome**: Clean, semantic typography with 85%+ AppText/AppTypography usage, all form labels consistent, no hardcoded sizes.
  </action>
  <verify>
    - dart analyze lib/profile/create_profile/create_profile_widget.dart (no errors)
    - grep -c "fontSize:" lib/profile/create_profile/create_profile_widget.dart < 3 (only a few edge cases remain)
    - grep -c "GoogleFonts.outfit" lib/profile/create_profile/create_profile_widget.dart = 0
    - grep -c "AppText\." lib/profile/create_profile/create_profile_widget.dart > 15
    - flutter run --debug and navigate to create profile - no crashes, visually similar
  </verify>
  <done>
    - All hardcoded fontSize replaced with AppText semantic constructors
    - GoogleFonts.outfit eliminated
    - Form labels consistent (all use AppText.label)
    - Screen compiles and runs without errors
    - 85%+ typography compliance achieved
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate sign_up_account_widget.dart (20% compliant → 85%+ target)</name>
  <files>lib/user_auth/sign_up_account/sign_up_account_widget.dart</files>
  <action>
**Current state**: Header fontSize: 32, heavy AppTheme.override anti-pattern usage, manual GoogleFonts.outfit throughout.

**Migration strategy**:
1. Replace screen title (fontSize: 32) → AppText.screenTitle (24px is standard, 32px was too large)
2. Find all AppTheme.override anti-patterns and replace:
   ```dart
   // FROM:
   AppTheme.of(context).titleMedium.override(
     font: GoogleFonts.outfit(...),
     fontSize: 18.0,
     ...
   )

   // TO:
   AppText.cardTitle(text)  // or appropriate semantic constructor
   ```

3. Replace auth form elements:
   - Form field labels → AppText.label
   - Helper text → AppText.helper
   - Error text → AppText.error
   - Button text → Already handled by AppButtonEnhanced, but if manual Text exists → AppText.button

4. Remove ALL GoogleFonts.outfit() calls
5. Ensure proper text hierarchy: title > labels > body > helper

**Do NOT**:
- Modify auth logic
- Change form validation
- Alter layout structure
- Modify navigation

**Expected outcome**: Clean auth screen with standard 24px title, consistent form labels, no anti-patterns.
  </action>
  <verify>
    - dart analyze lib/user_auth/sign_up_account/sign_up_account_widget.dart (no errors)
    - grep -c "override(" lib/user_auth/sign_up_account/sign_up_account_widget.dart = 0
    - grep -c "GoogleFonts.outfit" lib/user_auth/sign_up_account/sign_up_account_widget.dart = 0
    - grep -c "AppText\." lib/user_auth/sign_up_account/sign_up_account_widget.dart > 10
    - Screen title is 24px (AppText.screenTitle), not 32px
  </verify>
  <done>
    - AppTheme.override anti-pattern eliminated
    - Screen title standardized to 24px
    - All form elements use semantic typography
    - GoogleFonts.outfit removed
    - 85%+ typography compliance achieved
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate game_chat_details_widget.dart (25% compliant → 85%+ target)</name>
  <files>lib/chat_group/game_chat_details/game_chat_details_widget.dart</files>
  <action>
**Current state**: Extreme size range (10px to 32px), message text fontSize: 16, timestamps fontSize: 11, hero number fontSize: 32.

**Migration strategy**:
1. Replace typography by message element:
   - Chat messages (fontSize: 16) → AppText.body
   - Timestamps (fontSize: 11) → AppText.timestamp (uses 12px caption with secondary color, close enough)
   - Sender names (varies) → AppText.label or AppText.bodySmall (depending on prominence)
   - Hero numbers/scores (fontSize: 32) → AppText.monoLarge (48px) or AppText.monoMedium (20px) depending on context
   - System messages (if any) → AppText.caption with appropriate color
   - Chat metadata → AppText.caption or AppText.timestamp

2. For scores/stats, use mono styles:
   - Large scores → AppText.monoLarge or AppText.monoDisplay
   - Small stats → AppText.monoSmall or AppText.monoMedium

3. Ensure chat bubble text hierarchy: sender name (label) > message (body) > timestamp (timestamp)

4. Remove hardcoded fontSize values
5. Remove GoogleFonts.outfit if present

**Do NOT**:
- Modify chat functionality
- Change message rendering logic
- Alter real-time updates
- Modify scroll behavior

**Expected outcome**: Chat messages with consistent typography, proper mono font for scores, readable timestamps at 12px.
  </action>
  <verify>
    - dart analyze lib/chat_group/game_chat_details/game_chat_details_widget.dart (no errors)
    - grep -c "fontSize:" lib/chat_group/game_chat_details/game_chat_details_widget.dart < 2
    - Chat messages use AppText.body
    - Timestamps use AppText.timestamp or AppText.caption
    - Scores use AppText.mono* styles
  </verify>
  <done>
    - Message text standardized with AppText.body
    - Timestamps use AppText.timestamp (12px)
    - Scores use mono typography
    - Hardcoded sizes eliminated
    - 85%+ typography compliance achieved
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate edit_profile_widget.dart (25% compliant → 85%+ target)</name>
  <files>lib/profile/edit_profile/edit_profile_widget.dart</files>
  <action>
**Current state**: Similar issues to create_profile - hardcoded sizes, manual GoogleFonts usage.

**Migration strategy**:
1. Use same approach as create_profile (Task 1):
   - Screen title → AppText.screenTitle
   - Form sections → AppText.sectionHeader
   - Form labels → AppText.label
   - Helper text → AppText.helper
   - Body text → AppText.body

2. Ensure consistency with create_profile typography (both are profile forms, should match)

3. Remove GoogleFonts.outfit calls
4. Remove hardcoded fontSize values
5. Standardize all form labels to same size (AppText.label)

**Do NOT**:
- Modify profile update logic
- Change image upload handling
- Alter validation
- Modify Firebase updates

**Expected outcome**: Edit profile form matching create profile typography exactly, clean semantic styles throughout.
  </action>
  <verify>
    - dart analyze lib/profile/edit_profile/edit_profile_widget.dart (no errors)
    - grep -c "fontSize:" lib/profile/edit_profile/edit_profile_widget.dart < 3
    - grep -c "GoogleFonts.outfit" lib/profile/edit_profile/edit_profile_widget.dart = 0
    - Typography matches create_profile patterns
  </verify>
  <done>
    - Form typography matches create_profile
    - All hardcoded sizes replaced
    - GoogleFonts.outfit eliminated
    - Semantic AppText usage throughout
    - 85%+ typography compliance achieved
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate create_game_widget.dart (30% compliant → 85%+ target)</name>
  <files>lib/main_function/create_game/create_game_widget.dart</files>
  <action>
**Current state**: 13+ hardcoded sizes (13-22px), section headers inconsistent (16px vs 18px), help dialog uses hardcoded 22px header + 15px body.

**Migration strategy**:
1. Standardize screen structure:
   - Screen title → AppText.screenTitle (24px)
   - Form sections → AppText.sectionHeader (20px titleLarge)
   - Form labels → AppText.label
   - Helper text → AppText.helper
   - Dropdown labels → AppText.label
   - Descriptions → AppText.body or AppText.bodySmall

2. Fix help dialog typography:
   - Dialog title (fontSize: 22) → AppText.sectionHeader (20px close enough, or use 22px transition helper)
   - Dialog body (fontSize: 15) → AppText.body (16px)

3. Ensure all section headers are same size (currently mix of 16px and 18px) → all become AppText.sectionHeader (20px)

4. Remove GoogleFonts.outfit calls
5. Remove hardcoded fontSize values

**Do NOT**:
- Modify game creation logic
- Change form validation
- Alter date/time pickers
- Modify Firestore writes

**Expected outcome**: Create game form with consistent section headers, clean form labels, standardized typography throughout.
  </action>
  <verify>
    - dart analyze lib/main_function/create_game/create_game_widget.dart (no errors)
    - grep -c "fontSize:" lib/main_function/create_game/create_game_widget.dart < 3
    - All section headers same size (AppText.sectionHeader)
    - Help dialog uses semantic typography
    - Form labels consistent (all AppText.label)
  </verify>
  <done>
    - Section headers standardized to 20px
    - Form labels consistent
    - Help dialog uses semantic styles
    - Hardcoded sizes eliminated
    - 85%+ typography compliance achieved
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 5 files compile with dart analyze (no errors)
- [ ] grep "fontSize:" across all 5 files shows <15 total instances (down from ~70+)
- [ ] grep "GoogleFonts.outfit" across all 5 files shows 0 instances
- [ ] grep "AppText\." across all 5 files shows 75+ instances
- [ ] flutter run and manually test each screen - no crashes, layouts intact
- [ ] Visual comparison: text hierarchy clearer, sizes more consistent
</verification>

<success_criteria>
- All 5 worst offender screens migrated from 15-30% to 85%+ compliance
- ~70+ hardcoded fontSize instances eliminated
- All GoogleFonts.outfit calls removed
- All AppTheme.override anti-patterns eliminated
- Semantic AppText usage established as standard pattern
- No compilation errors or runtime crashes
- Layouts visually similar (minor size adjustments acceptable)
</success_criteria>

<output>
After completion, create `.planning/phases/03-typography-system/03-02-SUMMARY.md`
</output>
