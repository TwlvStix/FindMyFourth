---
phase: 02-component-library-standardization
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/core/widgets/app_list_tile.dart
  - lib/core/widgets/app_avatar.dart
  - lib/core/widgets/app_empty_state.dart
  - lib/core/widgets/app_loading_state.dart
  - lib/core/widgets/app_stream_builder.dart
  - lib/core/widgets/app_info_grid.dart
  - lib/main_function/golfers/golfers_widget.dart
  - lib/friends/tab_friends_widget.dart
  - lib/main_function/games_joined/games_joined_widget.dart
autonomous: true
---

<objective>
Create 6 critical missing components (AppListTile, AppAvatar, AppEmptyState, AppLoadingState, AppStreamBuilder, AppInfoGrid) that block screen standardization.

Purpose: Unblock refactoring of 50+ screens that use list patterns. AppListTile alone affects players, friends, games, community, and chat screens.

Output: 6 new core components, 3-5 list screens migrated as proof, ~1,000 lines of duplicate code eliminated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# New tokens from Plan 02-01
@lib/core/design_tokens/border_radius.dart
@lib/core/design_tokens/elevation.dart
@lib/core/design_tokens/opacity.dart
@lib/core/design_tokens/icon_size.dart

# Existing tokens
@lib/core/design_tokens/colors.dart
@lib/core/design_tokens/spacing.dart
@lib/core/design_tokens/typography.dart

# Audit findings - list pattern duplication
@.planning/phases/01-foundation-audit/01-02-SUMMARY.md
@.planning/phases/01-foundation-audit/STANDARDIZATION-ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppListTile component (HIGHEST PRIORITY)</name>
  <files>lib/core/widgets/app_list_tile.dart</files>
  <action>
Create comprehensive list tile component to replace 30+ inline list item patterns.

**Component structure:**

```dart
import 'package:flutter/material.dart';
import '../design_tokens/colors.dart';
import '../design_tokens/typography.dart';
import '../design_tokens/border_radius.dart';
import '../design_tokens/spacing.dart';
import '../design_tokens/elevation.dart';
import 'app_avatar.dart';

enum AppListTileVariant {
  /// Standard list tile with divider
  standard,

  /// Compact tile for dense lists
  compact,

  /// Card-style tile with background
  card,

  /// Highlighted tile for selected/active state
  highlighted,
}

class AppListTile extends StatelessWidget {
  final String title;
  final String? subtitle;
  final String? trailing;
  final Widget? leadingWidget;
  final Widget? trailingWidget;
  final String? avatarUrl;
  final String? avatarInitials;
  final IconData? leadingIcon;
  final IconData? trailingIcon;
  final AppListTileVariant variant;
  final VoidCallback? onTap;
  final bool showDivider;

  const AppListTile({
    super.key,
    required this.title,
    this.subtitle,
    this.trailing,
    this.leadingWidget,
    this.trailingWidget,
    this.avatarUrl,
    this.avatarInitials,
    this.leadingIcon,
    this.trailingIcon,
    this.variant = AppListTileVariant.standard,
    this.onTap,
    this.showDivider = true,
  });

  @override
  Widget build(BuildContext context) {
    final content = _buildContent();

    switch (variant) {
      case AppListTileVariant.standard:
        return Column(
          children: [
            InkWell(
              onTap: onTap,
              child: Padding(
                padding: EdgeInsets.symmetric(
                  horizontal: AppSpacing.md,
                  vertical: AppSpacing.sm,
                ),
                child: content,
              ),
            ),
            if (showDivider)
              Divider(
                height: 1,
                thickness: 1,
                color: AppColors.cloud,
                indent: AppSpacing.md,
                endIndent: AppSpacing.md,
              ),
          ],
        );

      case AppListTileVariant.compact:
        return Column(
          children: [
            InkWell(
              onTap: onTap,
              child: Padding(
                padding: EdgeInsets.symmetric(
                  horizontal: AppSpacing.md,
                  vertical: AppSpacing.xs,
                ),
                child: content,
              ),
            ),
            if (showDivider)
              Divider(
                height: 1,
                thickness: 1,
                color: AppColors.cloud,
                indent: AppSpacing.md,
                endIndent: AppSpacing.md,
              ),
          ],
        );

      case AppListTileVariant.card:
        return Padding(
          padding: EdgeInsets.symmetric(
            horizontal: AppSpacing.md,
            vertical: AppSpacing.xs,
          ),
          child: Material(
            color: AppColors.pure,
            borderRadius: BorderRadius.circular(AppBorderRadius.md),
            elevation: 0,
            child: InkWell(
              onTap: onTap,
              borderRadius: BorderRadius.circular(AppBorderRadius.md),
              child: Container(
                decoration: BoxDecoration(
                  border: Border.all(color: AppColors.cloud),
                  borderRadius: BorderRadius.circular(AppBorderRadius.md),
                ),
                padding: EdgeInsets.all(AppSpacing.md),
                child: content,
              ),
            ),
          ),
        );

      case AppListTileVariant.highlighted:
        return Padding(
          padding: EdgeInsets.symmetric(
            horizontal: AppSpacing.md,
            vertical: AppSpacing.xs,
          ),
          child: Material(
            color: AppColors.fairwayLight.withOpacity(0.1),
            borderRadius: BorderRadius.circular(AppBorderRadius.md),
            child: InkWell(
              onTap: onTap,
              borderRadius: BorderRadius.circular(AppBorderRadius.md),
              child: Container(
                decoration: BoxDecoration(
                  border: Border.all(
                    color: AppColors.fairway.withOpacity(0.3),
                  ),
                  borderRadius: BorderRadius.circular(AppBorderRadius.md),
                ),
                padding: EdgeInsets.all(AppSpacing.md),
                child: content,
              ),
            ),
          ),
        );
    }
  }

  Widget _buildContent() {
    return Row(
      children: [
        if (leadingWidget != null)
          leadingWidget!
        else if (avatarUrl != null || avatarInitials != null)
          AppAvatar(
            imageUrl: avatarUrl,
            initials: avatarInitials,
            size: variant == AppListTileVariant.compact
                ? AppAvatarSize.small
                : AppAvatarSize.medium,
          )
        else if (leadingIcon != null)
          Container(
            width: 40,
            height: 40,
            decoration: BoxDecoration(
              color: AppColors.sand,
              borderRadius: BorderRadius.circular(AppBorderRadius.sm),
            ),
            child: Icon(
              leadingIcon,
              color: AppColors.slate,
              size: 20,
            ),
          ),
        if (leadingWidget != null ||
            avatarUrl != null ||
            avatarInitials != null ||
            leadingIcon != null)
          SizedBox(width: AppSpacing.sm),
        Expanded(
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text(
                title,
                style: variant == AppListTileVariant.compact
                    ? AppTypography.bodyMedium
                    : AppTypography.titleMedium,
                maxLines: 1,
                overflow: TextOverflow.ellipsis,
              ),
              if (subtitle != null) ...[
                SizedBox(height: AppSpacing.xxs),
                Text(
                  subtitle!,
                  style: AppTypography.bodySmall.copyWith(
                    color: AppColors.stone,
                  ),
                  maxLines: variant == AppListTileVariant.compact ? 1 : 2,
                  overflow: TextOverflow.ellipsis,
                ),
              ],
            ],
          ),
        ),
        if (trailing != null || trailingWidget != null || trailingIcon != null)
          SizedBox(width: AppSpacing.sm),
        if (trailingWidget != null)
          trailingWidget!
        else if (trailing != null)
          Text(
            trailing!,
            style: AppTypography.bodySmall.copyWith(
              color: AppColors.stone,
            ),
          )
        else if (trailingIcon != null)
          Icon(
            trailingIcon,
            color: AppColors.slate,
            size: 20,
          ),
      ],
    );
  }
}
```

**Key features:**
- 4 variants: standard (default lists), compact (dense data), card (standalone items), highlighted (selected state)
- Flexible leading content: avatar, icon, or custom widget
- Flexible trailing content: text, icon, or custom widget
- Optional divider
- Integrated with design tokens
- Tap handling with ink ripple

**Audit context:**
- List item pattern duplicated 30+ times (players, friends, games, chat)
- Inconsistent avatar sizes and spacing
- No standard pattern for highlighted/selected states
- This component alone unblocks 50+ screens
  </action>
  <verify>app_list_tile.dart exists, dart analyze shows no errors, all 4 variants render correctly</verify>
  <done>AppListTile created with 4 variants, ready for mass migration</done>
</task>

<task type="auto">
  <name>Task 2: Create AppAvatar component</name>
  <files>lib/core/widgets/app_avatar.dart</files>
  <action>
Create avatar component with sizes and shapes.

**Component structure:**

```dart
import 'package:flutter/material.dart';
import '../design_tokens/colors.dart';
import '../design_tokens/typography.dart';
import '../design_tokens/border_radius.dart';

enum AppAvatarSize {
  /// Extra small (24x24) - list items in compact mode
  xs,

  /// Small (32x32) - compact lists, inline mentions
  small,

  /// Medium (40x40) - standard list items
  medium,

  /// Large (56x56) - profile headers, featured users
  large,

  /// Extra large (72x72) - profile pages
  xl,

  /// Extra extra large (96x96) - profile hero sections
  xxl,
}

enum AppAvatarShape {
  /// Circular avatar (default)
  circle,

  /// Rounded square
  rounded,

  /// Square avatar
  square,
}

class AppAvatar extends StatelessWidget {
  final String? imageUrl;
  final String? initials;
  final AppAvatarSize size;
  final AppAvatarShape shape;
  final Color? backgroundColor;

  const AppAvatar({
    super.key,
    this.imageUrl,
    this.initials,
    this.size = AppAvatarSize.medium,
    this.shape = AppAvatarShape.circle,
    this.backgroundColor,
  });

  @override
  Widget build(BuildContext context) {
    final dimension = _getDimension();
    final borderRadius = _getBorderRadius();
    final bgColor = backgroundColor ?? AppColors.fairway;

    return Container(
      width: dimension,
      height: dimension,
      decoration: BoxDecoration(
        color: imageUrl != null ? null : bgColor,
        borderRadius: borderRadius,
        image: imageUrl != null
            ? DecorationImage(
                image: NetworkImage(imageUrl!),
                fit: BoxFit.cover,
              )
            : null,
      ),
      child: imageUrl == null && initials != null
          ? Center(
              child: Text(
                initials!,
                style: _getTextStyle().copyWith(
                  color: AppColors.pure,
                ),
              ),
            )
          : null,
    );
  }

  double _getDimension() {
    switch (size) {
      case AppAvatarSize.xs:
        return 24;
      case AppAvatarSize.small:
        return 32;
      case AppAvatarSize.medium:
        return 40;
      case AppAvatarSize.large:
        return 56;
      case AppAvatarSize.xl:
        return 72;
      case AppAvatarSize.xxl:
        return 96;
    }
  }

  BorderRadius? _getBorderRadius() {
    switch (shape) {
      case AppAvatarShape.circle:
        return BorderRadius.circular(999); // Full circle
      case AppAvatarShape.rounded:
        return BorderRadius.circular(AppBorderRadius.md);
      case AppAvatarShape.square:
        return null;
    }
  }

  TextStyle _getTextStyle() {
    switch (size) {
      case AppAvatarSize.xs:
        return AppTypography.labelSmall;
      case AppAvatarSize.small:
        return AppTypography.labelMedium;
      case AppAvatarSize.medium:
        return AppTypography.titleMedium;
      case AppAvatarSize.large:
        return AppTypography.titleLarge;
      case AppAvatarSize.xl:
        return AppTypography.headlineSmall;
      case AppAvatarSize.xxl:
        return AppTypography.headlineMedium;
    }
  }
}
```

**Key features:**
- 6 sizes (xs through xxl)
- 3 shapes (circle, rounded, square)
- Network image support with fallback to initials
- Customizable background color
- Integrated with design tokens

**Audit context:**
- Avatar sizes range from 24px to 96px with inconsistent implementation
- Some use CircleAvatar, others use Container+ClipRRect
- No standard shape variants
  </action>
  <verify>app_avatar.dart exists, dart analyze shows no errors, all sizes and shapes render correctly</verify>
  <done>AppAvatar created with 6 sizes and 3 shapes</done>
</task>

<task type="auto">
  <name>Task 3: Create AppEmptyState and AppLoadingState components</name>
  <files>
    lib/core/widgets/app_empty_state.dart,
    lib/core/widgets/app_loading_state.dart
  </files>
  <action>
Create state management components to replace 37 duplications.

**AppEmptyState (app_empty_state.dart):**

```dart
import 'package:flutter/material.dart';
import '../design_tokens/colors.dart';
import '../design_tokens/typography.dart';
import '../design_tokens/spacing.dart';

class AppEmptyState extends StatelessWidget {
  final IconData icon;
  final String title;
  final String? message;
  final String? actionText;
  final VoidCallback? onAction;

  const AppEmptyState({
    super.key,
    required this.icon,
    required this.title,
    this.message,
    this.actionText,
    this.onAction,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Padding(
        padding: EdgeInsets.all(AppSpacing.xl),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Icon(
              icon,
              size: 64,
              color: AppColors.cloud,
            ),
            SizedBox(height: AppSpacing.md),
            Text(
              title,
              style: AppTypography.titleLarge.copyWith(
                color: AppColors.slate,
              ),
              textAlign: TextAlign.center,
            ),
            if (message != null) ...[
              SizedBox(height: AppSpacing.sm),
              Text(
                message!,
                style: AppTypography.bodyMedium.copyWith(
                  color: AppColors.stone,
                ),
                textAlign: TextAlign.center,
              ),
            ],
            if (actionText != null && onAction != null) ...[
              SizedBox(height: AppSpacing.lg),
              TextButton(
                onPressed: onAction,
                child: Text(actionText!),
              ),
            ],
          ],
        ),
      ),
    );
  }
}
```

**AppLoadingState (app_loading_state.dart):**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_spinkit/flutter_spinkit.dart';
import '../design_tokens/colors.dart';
import '../design_tokens/typography.dart';
import '../design_tokens/spacing.dart';

enum AppLoadingVariant {
  /// Spinner only
  spinner,

  /// Spinner with message
  message,

  /// Linear progress indicator
  linear,
}

class AppLoadingState extends StatelessWidget {
  final AppLoadingVariant variant;
  final String? message;

  const AppLoadingState({
    super.key,
    this.variant = AppLoadingVariant.spinner,
    this.message,
  });

  const AppLoadingState.message(String this.message, {super.key})
      : variant = AppLoadingVariant.message;

  const AppLoadingState.linear({super.key, this.message})
      : variant = AppLoadingVariant.linear;

  @override
  Widget build(BuildContext context) {
    switch (variant) {
      case AppLoadingVariant.spinner:
        return Center(
          child: SpinKitFadingCircle(
            color: AppColors.fairway,
            size: 50.0,
          ),
        );

      case AppLoadingVariant.message:
        return Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              SpinKitFadingCircle(
                color: AppColors.fairway,
                size: 50.0,
              ),
              if (message != null) ...[
                SizedBox(height: AppSpacing.md),
                Text(
                  message!,
                  style: AppTypography.bodyMedium.copyWith(
                    color: AppColors.stone,
                  ),
                  textAlign: TextAlign.center,
                ),
              ],
            ],
          ),
        );

      case AppLoadingVariant.linear:
        return Column(
          children: [
            LinearProgressIndicator(
              color: AppColors.fairway,
              backgroundColor: AppColors.sand,
            ),
            if (message != null) ...[
              SizedBox(height: AppSpacing.md),
              Padding(
                padding: EdgeInsets.symmetric(horizontal: AppSpacing.md),
                child: Text(
                  message!,
                  style: AppTypography.bodySmall.copyWith(
                    color: AppColors.stone,
                  ),
                  textAlign: TextAlign.center,
                ),
              ),
            ],
          ],
        );
    }
  }
}
```

**Audit context:**
- Empty state duplicated 15+ times (different icons, messages)
- Loading state duplicated 22+ times (SpinKit variations)
- No standard patterns for empty/loading UX
  </action>
  <verify>Both files exist, dart analyze shows no errors, all variants render correctly</verify>
  <done>AppEmptyState and AppLoadingState created, ready to replace 37 duplications</done>
</task>

<task type="auto">
  <name>Task 4: Create AppStreamBuilder and AppInfoGrid components</name>
  <files>
    lib/core/widgets/app_stream_builder.dart,
    lib/core/widgets/app_info_grid.dart
  </files>
  <action>
Create specialized components for common patterns.

**AppStreamBuilder (app_stream_builder.dart):**

```dart
import 'package:flutter/material.dart';
import 'app_loading_state.dart';
import 'app_empty_state.dart';

class AppStreamBuilder<T> extends StatelessWidget {
  final Stream<T> stream;
  final Widget Function(BuildContext context, T data) builder;
  final String? loadingMessage;
  final IconData? emptyIcon;
  final String? emptyTitle;
  final String? emptyMessage;

  const AppStreamBuilder({
    super.key,
    required this.stream,
    required this.builder,
    this.loadingMessage,
    this.emptyIcon,
    this.emptyTitle,
    this.emptyMessage,
  });

  @override
  Widget build(BuildContext context) {
    return StreamBuilder<T>(
      stream: stream,
      builder: (context, snapshot) {
        if (snapshot.hasError) {
          return AppEmptyState(
            icon: Icons.error_outline,
            title: 'Something went wrong',
            message: snapshot.error.toString(),
          );
        }

        if (!snapshot.hasData) {
          return AppLoadingState(
            variant: loadingMessage != null
                ? AppLoadingVariant.message
                : AppLoadingVariant.spinner,
            message: loadingMessage,
          );
        }

        final data = snapshot.data as T;

        // Check if data is empty list
        if (data is List && data.isEmpty) {
          return AppEmptyState(
            icon: emptyIcon ?? Icons.inbox_outlined,
            title: emptyTitle ?? 'No items',
            message: emptyMessage,
          );
        }

        return builder(context, data);
      },
    );
  }
}
```

**AppInfoGrid (app_info_grid.dart):**

```dart
import 'package:flutter/material.dart';
import '../design_tokens/colors.dart';
import '../design_tokens/typography.dart';
import '../design_tokens/spacing.dart';
import '../design_tokens/border_radius.dart';

class AppInfoGrid extends StatelessWidget {
  final List<AppInfoGridItem> items;

  const AppInfoGrid({
    super.key,
    required this.items,
  });

  @override
  Widget build(BuildContext context) {
    return GridView.builder(
      shrinkWrap: true,
      physics: const NeverScrollableScrollPhysics(),
      gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(
        crossAxisCount: 2,
        childAspectRatio: 2,
        crossAxisSpacing: AppSpacing.sm,
        mainAxisSpacing: AppSpacing.sm,
      ),
      itemCount: items.length,
      itemBuilder: (context, index) => _buildItem(items[index]),
    );
  }

  Widget _buildItem(AppInfoGridItem item) {
    return Container(
      padding: EdgeInsets.all(AppSpacing.md),
      decoration: BoxDecoration(
        color: AppColors.sand,
        borderRadius: BorderRadius.circular(AppBorderRadius.md),
        border: Border.all(color: AppColors.cloud),
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Text(
            item.label,
            style: AppTypography.labelSmall.copyWith(
              color: AppColors.stone,
            ),
          ),
          SizedBox(height: AppSpacing.xxs),
          Text(
            item.value,
            style: AppTypography.titleLarge.copyWith(
              color: AppColors.onyx,
            ),
            maxLines: 1,
            overflow: TextOverflow.ellipsis,
          ),
        ],
      ),
    );
  }
}

class AppInfoGridItem {
  final String label;
  final String value;

  const AppInfoGridItem({
    required this.label,
    required this.value,
  });
}
```

**Audit context:**
- StreamBuilder boilerplate repeated 100+ times (loading/error/empty states)
- Info grid pattern found in profiles, game details (handicap, rounds played, etc.)
- No standard wrapper for Firebase real-time streams
  </action>
  <verify>Both files exist, dart analyze shows no errors, AppStreamBuilder handles all states correctly</verify>
  <done>AppStreamBuilder and AppInfoGrid created for specialized patterns</done>
</task>

<task type="auto">
  <name>Task 5: Migrate 3 list screens to AppListTile</name>
  <files>
    lib/main_function/golfers/golfers_widget.dart,
    lib/friends/tab_friends_widget.dart,
    lib/main_function/games_joined/games_joined_widget.dart
  </files>
  <action>
Migrate 3 high-visibility list screens to AppListTile as proof of pattern.

**Target screens:**
1. **golfers_widget.dart** - Golfers directory (3rd nav tab, player list)
2. **tab_friends_widget.dart** - Friends list
3. **games_joined_widget.dart** - Joined games list (2nd nav tab)

**Migration pattern:**

```dart
// OLD - inline list item
GestureDetector(
  onTap: () => navigateToProfile(player),
  child: Container(
    padding: EdgeInsets.all(16),
    child: Row(
      children: [
        CircleAvatar(
          backgroundImage: NetworkImage(player.photoUrl),
          radius: 20,
        ),
        SizedBox(width: 12),
        Expanded(
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Text(
                player.name,
                style: TextStyle(fontSize: 16, fontWeight: FontWeight.w600),
              ),
              Text(
                player.handicap,
                style: TextStyle(fontSize: 14, color: Colors.grey),
              ),
            ],
          ),
        ),
      ],
    ),
  ),
)

// NEW - AppListTile
AppListTile(
  title: player.name,
  subtitle: player.handicap,
  avatarUrl: player.photoUrl,
  variant: AppListTileVariant.standard,
  onTap: () => navigateToProfile(player),
)
```

**Migration steps per screen:**

**1. golfers_widget.dart:**
- Find ListView.builder or similar list widget
- Replace inline Row+Avatar+Text with AppListTile
- Use variant: standard for default list
- Preserve existing onTap navigation
- Expected: 1 list item pattern → AppListTile, applies to all items in list

**2. tab_friends_widget.dart:**
- Replace friend list items with AppListTile
- Use variant: card for visual separation of friend items
- Preserve friend request status badges (keep custom trailing widget)
- Expected: 2-3 list item patterns (friends, pending requests)

**3. games_joined_widget.dart:**
- Replace game list items with AppListTile
- Use variant: highlighted for active/selected games
- Use custom trailingWidget for game status badges
- Expected: 1-2 list item patterns (active games, past games)

**Expected impact:**
- 3-5 list patterns migrated
- ~300 lines of duplicate list code eliminated
- Proves pattern works for different list contexts (users, friends, games)

Do NOT migrate all 50+ screens - just these 3 as proof. Mass migration happens in Phase 3-4.
  </action>
  <verify>3 screens use AppListTile, lists render correctly with avatars and content, onTap navigation works, dart analyze shows no errors</verify>
  <done>3 list screens migrated to AppListTile, ~300 lines eliminated, pattern proven</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 6 components exist (AppListTile, AppAvatar, AppEmptyState, AppLoadingState, AppStreamBuilder, AppInfoGrid)
- [ ] 3 screens migrated to AppListTile
- [ ] dart analyze shows no errors
- [ ] Manual test: Open migrated screens, verify lists render correctly
- [ ] Manual test: Test AppListTile variants (standard, compact, card, highlighted)
- [ ] Manual test: Test AppAvatar sizes and shapes
- [ ] Manual test: Test AppEmptyState and AppLoadingState
</verification>

<success_criteria>

- All tasks completed
- 6 critical missing components created
- 3 list screens migrated to AppListTile
- ~1,000 lines of duplicate code eliminated
- List pattern standardized across migrated screens
- No errors or warnings introduced
- Foundation ready for mass migration in Phase 3-4
  </success_criteria>

<output>
After completion, create `.planning/phases/02-component-library-standardization/02-04-SUMMARY.md`
</output>
