---
phase: 02-component-library-standardization
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/core/widgets/app_text_field.dart
  - lib/core/widgets/app_badge.dart
  - lib/main_function/create_game/create_game_widget.dart
  - lib/profile/edit_profile_widget.dart
  - lib/chat_group/chat_widget.dart
  - lib/user_auth/sign_up_account_widget.dart
autonomous: true
---

<objective>
Create AppTextField and AppBadge components to standardize input fields and status indicators across the app.

Purpose: Eliminate form/input inconsistencies by providing standard text field component with validation states, and create semantic badge system for status indicators.

Output: AppTextField component with 4 variants, AppBadge component with semantic states, 4-5 forms migrated to AppTextField as proof.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# New tokens from Plan 02-01
@lib/core/design_tokens/border_radius.dart
@lib/core/design_tokens/elevation.dart
@lib/core/design_tokens/opacity.dart

# Existing tokens
@lib/core/design_tokens/colors.dart
@lib/core/design_tokens/spacing.dart
@lib/core/design_tokens/typography.dart

# Audit findings
@.planning/phases/01-foundation-audit/01-02-SUMMARY.md
@.planning/phases/01-foundation-audit/STANDARDIZATION-ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppTextField component</name>
  <files>lib/core/widgets/app_text_field.dart</files>
  <action>
Create comprehensive text field component following Flutter's TextFormField patterns with design token integration.

**Component structure:**

```dart
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';
import '../design_tokens/colors.dart';
import '../design_tokens/typography.dart';
import '../design_tokens/border_radius.dart';
import '../design_tokens/spacing.dart';

enum AppTextFieldVariant {
  /// Outlined border style (default)
  outlined,

  /// Filled background style
  filled,

  /// Underlined style (minimal)
  underlined,

  /// Search field with search icon
  search,
}

class AppTextField extends StatefulWidget {
  final String? label;
  final String? hint;
  final String? helperText;
  final String? errorText;
  final String? initialValue;
  final TextEditingController? controller;
  final AppTextFieldVariant variant;
  final bool obscureText;
  final bool enabled;
  final bool readOnly;
  final int? maxLines;
  final int? maxLength;
  final TextInputType? keyboardType;
  final List<TextInputFormatter>? inputFormatters;
  final String? Function(String?)? validator;
  final void Function(String)? onChanged;
  final void Function(String)? onSubmitted;
  final IconData? prefixIcon;
  final IconData? suffixIcon;
  final VoidCallback? onSuffixIconTap;

  const AppTextField({
    super.key,
    this.label,
    this.hint,
    this.helperText,
    this.errorText,
    this.initialValue,
    this.controller,
    this.variant = AppTextFieldVariant.outlined,
    this.obscureText = false,
    this.enabled = true,
    this.readOnly = false,
    this.maxLines = 1,
    this.maxLength,
    this.keyboardType,
    this.inputFormatters,
    this.validator,
    this.onChanged,
    this.onSubmitted,
    this.prefixIcon,
    this.suffixIcon,
    this.onSuffixIconTap,
  });

  @override
  State<AppTextField> createState() => _AppTextFieldState();
}

class _AppTextFieldState extends State<AppTextField> {
  late FocusNode _focusNode;
  bool _isFocused = false;

  @override
  void initState() {
    super.initState();
    _focusNode = FocusNode();
    _focusNode.addListener(_onFocusChange);
  }

  void _onFocusChange() {
    setState(() {
      _isFocused = _focusNode.hasFocus;
    });
  }

  @override
  void dispose() {
    _focusNode.removeListener(_onFocusChange);
    _focusNode.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final hasError = widget.errorText != null;

    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        if (widget.label != null) ...[
          Text(
            widget.label!,
            style: AppTypography.labelMedium.copyWith(
              color: AppColors.slate,
            ),
          ),
          SizedBox(height: AppSpacing.xxs),
        ],
        TextFormField(
          controller: widget.controller,
          initialValue: widget.initialValue,
          focusNode: _focusNode,
          obscureText: widget.obscureText,
          enabled: widget.enabled,
          readOnly: widget.readOnly,
          maxLines: widget.maxLines,
          maxLength: widget.maxLength,
          keyboardType: widget.keyboardType,
          inputFormatters: widget.inputFormatters,
          validator: widget.validator,
          onChanged: widget.onChanged,
          onFieldSubmitted: widget.onSubmitted,
          style: AppTypography.bodyMedium,
          decoration: _buildDecoration(hasError),
        ),
        if (widget.helperText != null && !hasError) ...[
          SizedBox(height: AppSpacing.xxs),
          Text(
            widget.helperText!,
            style: AppTypography.bodySmall.copyWith(
              color: AppColors.stone,
            ),
          ),
        ],
        if (hasError) ...[
          SizedBox(height: AppSpacing.xxs),
          Text(
            widget.errorText!,
            style: AppTypography.bodySmall.copyWith(
              color: AppColors.error,
            ),
          ),
        ],
      ],
    );
  }

  InputDecoration _buildDecoration(bool hasError) {
    final borderColor = hasError
        ? AppColors.error
        : _isFocused
            ? AppColors.fairway
            : AppColors.cloud;

    switch (widget.variant) {
      case AppTextFieldVariant.outlined:
        return InputDecoration(
          hintText: widget.hint,
          hintStyle: AppTypography.bodyMedium.copyWith(
            color: AppColors.stone,
          ),
          prefixIcon: widget.prefixIcon != null
              ? Icon(widget.prefixIcon, color: AppColors.slate)
              : null,
          suffixIcon: widget.suffixIcon != null
              ? IconButton(
                  icon: Icon(widget.suffixIcon, color: AppColors.slate),
                  onPressed: widget.onSuffixIconTap,
                )
              : null,
          border: OutlineInputBorder(
            borderRadius: BorderRadius.circular(AppBorderRadius.sm),
            borderSide: BorderSide(color: AppColors.cloud),
          ),
          enabledBorder: OutlineInputBorder(
            borderRadius: BorderRadius.circular(AppBorderRadius.sm),
            borderSide: BorderSide(color: borderColor),
          ),
          focusedBorder: OutlineInputBorder(
            borderRadius: BorderRadius.circular(AppBorderRadius.sm),
            borderSide: BorderSide(color: borderColor, width: 2),
          ),
          errorBorder: OutlineInputBorder(
            borderRadius: BorderRadius.circular(AppBorderRadius.sm),
            borderSide: BorderSide(color: AppColors.error),
          ),
          contentPadding: EdgeInsets.symmetric(
            horizontal: AppSpacing.md,
            vertical: AppSpacing.sm,
          ),
        );

      case AppTextFieldVariant.filled:
        return InputDecoration(
          hintText: widget.hint,
          hintStyle: AppTypography.bodyMedium.copyWith(
            color: AppColors.stone,
          ),
          prefixIcon: widget.prefixIcon != null
              ? Icon(widget.prefixIcon, color: AppColors.slate)
              : null,
          suffixIcon: widget.suffixIcon != null
              ? IconButton(
                  icon: Icon(widget.suffixIcon, color: AppColors.slate),
                  onPressed: widget.onSuffixIconTap,
                )
              : null,
          filled: true,
          fillColor: AppColors.sand,
          border: OutlineInputBorder(
            borderRadius: BorderRadius.circular(AppBorderRadius.sm),
            borderSide: BorderSide.none,
          ),
          focusedBorder: OutlineInputBorder(
            borderRadius: BorderRadius.circular(AppBorderRadius.sm),
            borderSide: BorderSide(color: borderColor, width: 2),
          ),
          contentPadding: EdgeInsets.symmetric(
            horizontal: AppSpacing.md,
            vertical: AppSpacing.sm,
          ),
        );

      case AppTextFieldVariant.underlined:
        return InputDecoration(
          hintText: widget.hint,
          hintStyle: AppTypography.bodyMedium.copyWith(
            color: AppColors.stone,
          ),
          prefixIcon: widget.prefixIcon != null
              ? Icon(widget.prefixIcon, color: AppColors.slate)
              : null,
          suffixIcon: widget.suffixIcon != null
              ? IconButton(
                  icon: Icon(widget.suffixIcon, color: AppColors.slate),
                  onPressed: widget.onSuffixIconTap,
                )
              : null,
          border: UnderlineInputBorder(
            borderSide: BorderSide(color: AppColors.cloud),
          ),
          enabledBorder: UnderlineInputBorder(
            borderSide: BorderSide(color: borderColor),
          ),
          focusedBorder: UnderlineInputBorder(
            borderSide: BorderSide(color: borderColor, width: 2),
          ),
          contentPadding: EdgeInsets.symmetric(
            vertical: AppSpacing.sm,
          ),
        );

      case AppTextFieldVariant.search:
        return InputDecoration(
          hintText: widget.hint ?? 'Search...',
          hintStyle: AppTypography.bodyMedium.copyWith(
            color: AppColors.stone,
          ),
          prefixIcon: Icon(Icons.search, color: AppColors.slate),
          suffixIcon: widget.suffixIcon != null
              ? IconButton(
                  icon: Icon(widget.suffixIcon, color: AppColors.slate),
                  onPressed: widget.onSuffixIconTap,
                )
              : null,
          filled: true,
          fillColor: AppColors.sand,
          border: OutlineInputBorder(
            borderRadius: BorderRadius.circular(AppBorderRadius.full),
            borderSide: BorderSide.none,
          ),
          focusedBorder: OutlineInputBorder(
            borderRadius: BorderRadius.circular(AppBorderRadius.full),
            borderSide: BorderSide(color: borderColor, width: 2),
          ),
          contentPadding: EdgeInsets.symmetric(
            horizontal: AppSpacing.md,
            vertical: AppSpacing.sm,
          ),
        );
    }
  }
}
```

**Key features:**
- 4 variants: outlined (default), filled, underlined, search
- Built-in validation states with error text
- Focus states with color changes
- Optional prefix/suffix icons
- Integrated with all design tokens
- Full TextFormField compatibility

**Audit context:**
- Input fields have inconsistent styling across app
- Some use hardcoded colors, others use theme
- No standard pattern for validation errors
- Search fields have varying styles
  </action>
  <verify>app_text_field.dart exists, dart analyze shows no errors, all 4 variants render correctly, focus states work</verify>
  <done>AppTextField created with 4 variants and full validation support</done>
</task>

<task type="auto">
  <name>Task 2: Create AppBadge component</name>
  <files>lib/core/widgets/app_badge.dart</files>
  <action>
Create semantic badge component for status indicators.

**Component structure:**

```dart
import 'package:flutter/material.dart';
import '../design_tokens/colors.dart';
import '../design_tokens/typography.dart';
import '../design_tokens/border_radius.dart';
import '../design_tokens/spacing.dart';

enum AppBadgeVariant {
  /// Success state (green) - completed, confirmed, active
  success,

  /// Warning state (gold) - pending, in-progress, attention
  warning,

  /// Error state (red) - failed, rejected, cancelled
  error,

  /// Info state (blue) - informational, neutral status
  info,

  /// Primary state (fairway green) - brand-colored badges
  primary,

  /// Subtle state (grey) - low-emphasis status
  subtle,
}

enum AppBadgeSize {
  /// Small badge for compact UI
  small,

  /// Medium badge (default)
  medium,

  /// Large badge for emphasis
  large,
}

class AppBadge extends StatelessWidget {
  final String label;
  final AppBadgeVariant variant;
  final AppBadgeSize size;
  final IconData? icon;

  const AppBadge({
    super.key,
    required this.label,
    this.variant = AppBadgeVariant.primary,
    this.size = AppBadgeSize.medium,
    this.icon,
  });

  @override
  Widget build(BuildContext context) {
    final colors = _getColors();
    final textStyle = _getTextStyle();
    final padding = _getPadding();

    return Container(
      padding: padding,
      decoration: BoxDecoration(
        color: colors.background,
        borderRadius: BorderRadius.circular(AppBorderRadius.full),
        border: Border.all(
          color: colors.borderColor,
          width: 1,
        ),
      ),
      child: Row(
        mainAxisSize: MainAxisSize.min,
        children: [
          if (icon != null) ...[
            Icon(
              icon,
              color: colors.textColor,
              size: _getIconSize(),
            ),
            SizedBox(width: AppSpacing.xxs),
          ],
          Text(
            label,
            style: textStyle.copyWith(color: colors.textColor),
          ),
        ],
      ),
    );
  }

  EdgeInsets _getPadding() {
    switch (size) {
      case AppBadgeSize.small:
        return EdgeInsets.symmetric(
          horizontal: AppSpacing.xs,
          vertical: AppSpacing.xxs,
        );
      case AppBadgeSize.medium:
        return EdgeInsets.symmetric(
          horizontal: AppSpacing.sm,
          vertical: AppSpacing.xs,
        );
      case AppBadgeSize.large:
        return EdgeInsets.symmetric(
          horizontal: AppSpacing.md,
          vertical: AppSpacing.sm,
        );
    }
  }

  TextStyle _getTextStyle() {
    switch (size) {
      case AppBadgeSize.small:
        return AppTypography.labelSmall;
      case AppBadgeSize.medium:
        return AppTypography.labelMedium;
      case AppBadgeSize.large:
        return AppTypography.labelLarge;
    }
  }

  double _getIconSize() {
    switch (size) {
      case AppBadgeSize.small:
        return 12;
      case AppBadgeSize.medium:
        return 14;
      case AppBadgeSize.large:
        return 16;
    }
  }

  ({Color background, Color textColor, Color borderColor}) _getColors() {
    switch (variant) {
      case AppBadgeVariant.success:
        return (
          background: AppColors.success.withOpacity(0.1),
          textColor: AppColors.success,
          borderColor: AppColors.success.withOpacity(0.2),
        );
      case AppBadgeVariant.warning:
        return (
          background: AppColors.warning.withOpacity(0.1),
          textColor: Color(0xFFB8860B), // Darker gold for contrast
          borderColor: AppColors.warning.withOpacity(0.2),
        );
      case AppBadgeVariant.error:
        return (
          background: AppColors.error.withOpacity(0.1),
          textColor: AppColors.error,
          borderColor: AppColors.error.withOpacity(0.2),
        );
      case AppBadgeVariant.info:
        return (
          background: AppColors.info.withOpacity(0.1),
          textColor: AppColors.info,
          borderColor: AppColors.info.withOpacity(0.2),
        );
      case AppBadgeVariant.primary:
        return (
          background: AppColors.fairway.withOpacity(0.1),
          textColor: AppColors.fairway,
          borderColor: AppColors.fairway.withOpacity(0.2),
        );
      case AppBadgeVariant.subtle:
        return (
          background: AppColors.sand,
          textColor: AppColors.slate,
          borderColor: AppColors.cloud,
        );
    }
  }
}
```

**Key features:**
- 6 semantic variants (success, warning, error, info, primary, subtle)
- 3 sizes (small, medium, large)
- Optional leading icon
- Pill-shaped with full border radius
- Integrated with design tokens

**Audit context:**
- Status indicators have inconsistent colors and shapes
- No semantic color system for badges
- Some use hardcoded hex colors
  </action>
  <verify>app_badge.dart exists, dart analyze shows no errors, all 6 variants render correctly with proper colors</verify>
  <done>AppBadge created with 6 semantic variants and 3 sizes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate create_game form to AppTextField</name>
  <files>lib/main_function/create_game/create_game_widget.dart</files>
  <action>
Migrate the create game form to use AppTextField for all input fields.

**Target fields in create_game:**
1. Game name/title
2. Date/time picker (if text input)
3. Location/course name
4. Player count
5. Description/notes

**Migration pattern:**

```dart
// OLD - inline TextField or TextFormField
TextFormField(
  decoration: InputDecoration(
    labelText: 'Game Name',
    hintText: 'Enter game name...',
    border: OutlineInputBorder(
      borderRadius: BorderRadius.circular(8),
    ),
  ),
  validator: (value) {
    if (value == null || value.isEmpty) {
      return 'Game name is required';
    }
    return null;
  },
)

// NEW - AppTextField
AppTextField(
  label: 'Game Name',
  hint: 'Enter game name...',
  variant: AppTextFieldVariant.outlined,
  validator: (value) {
    if (value == null || value.isEmpty) {
      return 'Game name is required';
    }
    return null;
  },
)
```

**Steps:**
1. Add import: `import '/core/widgets/app_text_field.dart';`
2. Find all TextField and TextFormField instances
3. Replace with AppTextField
4. Choose appropriate variant (outlined for most, search for location picker)
5. Preserve existing validation logic
6. Preserve existing controllers and callbacks

**Expected impact:**
- 5-8 input fields migrated
- Consistent input styling across form
- Built-in error states and focus states
  </action>
  <verify>create_game_widget.dart uses AppTextField for all inputs, form validation works, dart analyze shows no errors</verify>
  <done>Create game form migrated to AppTextField</done>
</task>

<task type="auto">
  <name>Task 4: Migrate 2-3 more forms to AppTextField</name>
  <files>
    lib/profile/edit_profile_widget.dart,
    lib/chat_group/chat_widget.dart,
    lib/user_auth/sign_up_account_widget.dart
  </files>
  <action>
Migrate 2-3 additional forms to AppTextField as proof of pattern.

**Target screens:**
1. **edit_profile_widget.dart** - Profile editing form (name, bio, handicap)
2. **chat_widget.dart** - Chat message input field
3. **sign_up_account_widget.dart** - Sign-up form (email, password)

**Migration approach per screen:**

**1. edit_profile_widget.dart:**
- Name field → AppTextField(variant: outlined)
- Bio field → AppTextField(variant: outlined, maxLines: 4)
- Handicap field → AppTextField(variant: outlined, keyboardType: number)

**2. chat_widget.dart:**
- Message input → AppTextField(variant: filled, hint: 'Type a message...')
- Use filled variant for better contrast in chat UI

**3. sign_up_account_widget.dart:**
- Email field → AppTextField(variant: outlined, keyboardType: emailAddress, prefixIcon: Icons.email)
- Password field → AppTextField(variant: outlined, obscureText: true, prefixIcon: Icons.lock, suffixIcon: Icons.visibility)
- Add password visibility toggle using suffixIcon + onSuffixIconTap

**Steps for each:**
1. Add import for AppTextField
2. Replace TextField/TextFormField with AppTextField
3. Choose appropriate variant
4. Add validation if missing
5. Preserve controllers and state

**Expected impact:**
- 10-15 additional input fields migrated
- Proves pattern works across different contexts (forms, chat, auth)
- ~200 lines of duplicate input styling eliminated
  </action>
  <verify>3 screens use AppTextField, all inputs work correctly, validation states display properly, dart analyze shows no errors</verify>
  <done>3 additional screens migrated to AppTextField, pattern proven across contexts</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] AppTextField component exists with 4 variants
- [ ] AppBadge component exists with 6 variants
- [ ] 4 screens migrated to AppTextField (create_game + 3 others)
- [ ] dart analyze shows no errors
- [ ] Manual test: Fill out forms, verify validation works
- [ ] Manual test: Test all text field variants (outlined, filled, underlined, search)
- [ ] Manual test: Test all badge variants (success, warning, error, info, primary, subtle)
</verification>

<success_criteria>

- All tasks completed
- AppTextField created with 4 variants and full validation support
- AppBadge created with 6 semantic variants
- 4-5 forms migrated to AppTextField
- Input styling consistent across migrated screens
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/02-component-library-standardization/02-03-SUMMARY.md`
</output>
