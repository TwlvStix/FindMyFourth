---
phase: 11-prioritized-fixes-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/main_function/*/components/*.dart
  - lib/friends/*/components/*.dart
  - lib/backend/backend.dart
  - lib/providers/user_provider.dart
autonomous: true
---

<objective>
Eliminate 40 stream subscription leaks and add mounted checks to 197 setState calls after async operations.

Purpose: Fix critical memory leaks and crash-causing setState-after-dispose bugs before beta. These are the highest-volume stability issues identified in Phase 10-03 audit.
Output: Zero stream leaks, zero setState crashes, established async safety patterns.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 10-03 findings
@.planning/phases/10-state-management-error-handling-audit/10-03-SUMMARY.md
@.planning/phases/10-state-management-error-handling-audit/ASYNC-EDGE-CASES-AUDIT.md

# Existing patterns to follow
@lib/providers/user_provider.dart
@lib/providers/chat_provider.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stream subscription cleanup to 40 widgets missing dispose cancel()</name>
  <files>
    lib/main_function/game_joined_detailed/game_joined_detailed_widget.dart,
    lib/main_function/join_game_detailed/join_game_detailed_widget.dart,
    lib/main_function/create_game/create_game_widget.dart,
    lib/main_function/games_list/games_list_widget.dart,
    lib/main_function/games_joined/games_joined_widget.dart,
    lib/friends/tab_friends/tab_friends_widget.dart,
    lib/friends/golfers/golfers_widget.dart,
    lib/friends/profile_user_firebase/profile_user_firebase_widget.dart,
    lib/chat/game_chat_details/game_chat_details_widget.dart,
    lib/chat/chat/chat_widget.dart,
    (30 additional widgets from audit)
  </files>
  <action>
    For each widget with StreamSubscription fields:
    1. Find StreamSubscription fields (e.g., _userSubscription, _gameSubscription)
    2. In dispose() method, add: await _subscription?.cancel(); before super.dispose()
    3. Set to null after cancel: _subscription = null;
    4. Pattern from UserProvider: Store subscription, cancel in dispose, nullify
    5. Use try-finally if disposing during async: try { await _sub?.cancel(); } finally { _sub = null; }

    Priority order (fix highest-traffic widgets first):
    - Game detail screens (game_joined_detailed, join_game_detailed)
    - Chat screens (game_chat_details, chat)
    - Friend/social screens (tab_friends, golfers, profile_user_firebase)
    - List screens (games_list, games_joined)
    - Remaining 30 widgets from audit catalog

    Do NOT change logic, only add cleanup. Do NOT refactor widget structure.
  </action>
  <verify>
    grep -r "StreamSubscription" lib/ | grep -v "cancel()" | wc -l
    # Should be 0 (all subscriptions have cancel calls)

    # Verify pattern compliance
    grep -A 5 "dispose()" lib/main_function/game_joined_detailed/game_joined_detailed_widget.dart
    # Should show: await _subscription?.cancel(); _subscription = null;
  </verify>
  <done>
    - All 40 widgets have subscription cleanup in dispose()
    - Pattern: await subscription?.cancel(); subscription = null;
    - Zero remaining uncancelled subscriptions
    - No compilation errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add mounted checks before setState after all await calls</name>
  <files>
    lib/main_function/create_game/create_game_widget.dart,
    lib/main_function/game_joined_detailed/game_joined_detailed_widget.dart,
    lib/main_function/join_game_detailed/join_game_detailed_widget.dart,
    lib/friends/tab_friends/tab_friends_widget.dart,
    lib/friends/golfers/golfers_widget.dart,
    lib/auth/sign_up_account/sign_up_account_widget.dart,
    lib/auth/sign_in/sign_in_widget.dart,
    (48 additional files from audit)
  </files>
  <action>
    For each setState call that appears after an await:
    1. Find pattern: await someAsyncCall(); setState(() => ...);
    2. Add mounted check: if (!mounted) return; before setState
    3. Pattern: await call(); if (!mounted) return; setState(() => ...);
    4. For loading states, use try-finally: try { ... } finally { if (mounted) setState(() => _loading = false); }

    Focus on these async patterns (from audit):
    - Network requests (Firestore queries, API calls)
    - Navigation with await (context.pushNamed with await result)
    - Timer/Future.delayed callbacks
    - Image loading callbacks

    Priority files (highest setState volume):
    - create_game: 28 setState calls
    - game_joined_detailed: 22 setState calls
    - tab_friends: 18 setState calls
    - join_game_detailed: 16 setState calls
    - golfers: 14 setState calls

    Do NOT add mounted checks to synchronous setState (no await before it).
  </action>
  <verify>
    # Verify pattern in high-priority files
    grep -B 1 "setState" lib/main_function/create_game/create_game_widget.dart | grep -E "(await|mounted)"
    # Should show: await ... followed by if (!mounted) return; before setState

    # Check loading state cleanup pattern
    grep -A 3 "finally" lib/main_function/create_game/create_game_widget.dart | grep "mounted"
    # Should show: if (mounted) setState(() => _loading = false);
  </verify>
  <done>
    - All 197 setState calls after await have mounted checks
    - Pattern: if (!mounted) return; before setState
    - Loading states use: if (mounted) setState in finally blocks
    - Zero setState-after-dispose crashes possible
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace unsafe null assertions (!) with null-aware operators (??)</name>
  <files>
    lib/main_function/game_joined_detailed/game_joined_detailed_widget.dart,
    lib/main_function/join_game_detailed/join_game_detailed_widget.dart,
    lib/friends/tab_friends/tab_friends_widget.dart,
    lib/chat/game_chat_details/game_chat_details_widget.dart,
    lib/providers/user_provider.dart,
    (34 additional files from audit)
  </files>
  <action>
    Replace 66 null assertion operators (!) with null-safe alternatives:

    Pattern replacements:
    1. value! → value ?? defaultValue (provide safe default)
    2. list!.first → list?.first ?? defaultValue (safe access)
    3. map!['key'] → map?['key'] ?? defaultValue (safe map access)
    4. obj!.property → obj?.property ?? defaultValue (safe property access)

    Context-specific defaults:
    - Strings: ?? '' or ?? 'Unknown'
    - Numbers: ?? 0 or ?? -1 (sentinel)
    - Lists: ?? [] (empty list)
    - Booleans: ?? false
    - Objects: ?? fallbackObject or early return if null

    Priority: Error handling code paths (audit flagged ! in catch blocks as "ironic anti-pattern")

    Do NOT change logic, only replace ! with ??. If default value isn't obvious, use early return:
    if (value == null) return; // or continue, or show error

    Skip ! operators where null is truly impossible (after explicit null check).
  </action>
  <verify>
    # Count remaining ! operators (should reduce from 66 to ~10)
    grep -r "!" lib/ --include="*.dart" | grep -v "!=" | grep -v "!mounted" | wc -l
    # Should be <10 (only truly safe ! operators remain)

    # Verify null-aware patterns
    grep -r "??" lib/main_function/game_joined_detailed/ --include="*.dart" | wc -l
    # Should show increased usage of ?? operator
  </verify>
  <done>
    - 66 unsafe ! operators reduced to <10 justified cases
    - All replacements use ?? with appropriate defaults
    - Error handling paths no longer use ! (ironic anti-pattern eliminated)
    - Zero null-related crashes from replaced operators
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dart analyze` passes with zero errors
- [ ] `flutter test` passes (if tests exist)
- [ ] Spot-check 5 high-priority files for correct patterns
- [ ] No new warnings introduced
</verification>

<success_criteria>

- All 40 stream subscriptions have cancel() in dispose()
- All 197 setState calls after await have mounted checks
- 66 unsafe ! operators replaced with ?? (down to <10 justified cases)
- Zero memory leaks from uncancelled streams
- Zero setState-after-dispose crashes possible
- Established async safety patterns for future development
- Health score improvement: 62/100 → 75/100 (async safety category)
  </success_criteria>

<output>
After completion, create `.planning/phases/11-prioritized-fixes-refactoring/11-01-SUMMARY.md`
</output>
